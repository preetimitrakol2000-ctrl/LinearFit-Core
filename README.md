# LinearFit-Core

A fast statistical calculation structure implementing linear regression models inside standard C execution profiles. This project computes slopes ($m$) and intercept constants ($c$) manually to predict parametric trends.

## ⚡ Execution Architecture
```bash
gcc main.c regression.c -o linear_fit
./linear_fit
